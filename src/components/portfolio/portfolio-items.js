import React, { Component } from "react";

export default function(props) {
        return (
            <div>
                <h3>{props.title}</h3>
                <h4></h4>
            </div>
    );
}